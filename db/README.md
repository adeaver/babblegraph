# Babblegraph Database

To setup the database for the first time, run:
`docker-compose -f setup-compose.yml up --build` from the `db` directory

## TODO
- [ ] decide on a version of postgres
- [ ] build backup system for production
