export type Topic = {
    id: string;
    label: string;
    isActive: boolean;
}

export type TopicDisplayName = {
	id: string;
	topicId: string;
	languageCode: string;
	label: string;
	isActive: boolean;
}

