import React from 'react';
import ReactDOM from 'react-dom';

class App extends React.Component{
    render() {
        return (
            <div>
                <h1>Babblegraph.com is currently under construction</h1>
            </div>
        );
    }
}

ReactDOM.render(<App />, document.getElementById('content'));
